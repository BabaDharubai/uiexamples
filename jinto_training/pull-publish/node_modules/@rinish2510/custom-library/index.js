const { fill } = require("lodash");
const {conc} = require("./util");
var arr=[2];
var other=conc(1,2,333,44,arr);
// console.log(other)


const add=(a,b)=>{
    return a+b;
};

module.exports={
    add,
    fill
    
};